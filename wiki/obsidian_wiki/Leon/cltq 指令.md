---
title: cltq 指令（符号扩展）
tags:
  - 汇编
  - x86-64
  - 指令
---

# cltq —— 32 位下标扩展为 64 位地址

`cltq` 是为了把 32 位的 `int` 下标安全地转换成 64 位地址。它通过复制符号位到高 32 位，确保负数下标在 64 位寻址时依然正确，同时清除了高 32 位的垃圾数据，防止计算错地址。它把 `eax` 扩展成 `rax`。

> 对应 Intel 语法为 `cdqe`。

## 相关

- [[mov 指令后缀]] —— 操作数大小
- [[learning_c]] —— C 语言学习 MOC
