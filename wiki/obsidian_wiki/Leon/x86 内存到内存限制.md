---
title: x86-64 不支持内存到内存直接传送
tags:
  - 汇编
  - x86-64
  - 寻址
---

# x86-64 不支持「内存到内存」直接传送

x86-64 的指令集架构（ISA）在硬件层面就不支持“内存到内存”的直接数据传送。在 x86 体系下，绝大多数算术和逻辑运算指令（包括 `mov`）都要求至少有一个操作数必须是**寄存器**。CPU 的内部执行单元只能直接对寄存器里的数据进行运算或搬运。

## 相关

- [[lea 指令]] —— 地址计算
- [[mov 指令后缀]]
- [[learning_c]] —— C 语言学习 MOC
